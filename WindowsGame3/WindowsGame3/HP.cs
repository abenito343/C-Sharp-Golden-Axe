using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Audio;
using Microsoft.Xna.Framework.Content;
using Microsoft.Xna.Framework.GamerServices;
using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework.Input;
using Microsoft.Xna.Framework.Media;

namespace WindowsGame3
{
    /*class HP
    {
        Rectangle health;
        Rectangle Destino;
        Texture2D textura;
        int spriteAncho;
        int spriteAlto;
        Vector2 posicion = new Vector2();
        public HP(ContentManager Content, string nombre, Vector2 posicion)
        {
            this.textura = Content.Load<Texture2D>(nombre);
            posicion= new Vector2 (posicion.X,posicion.Y);  
        }
        public void Draw(SpriteBatch spriteBatch)
        {
            //HP = new Rectangle(textura,(int)posicion.X, (int)posicion.Y,Color.White);
        }
    }*/
}
